<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sono:wght@400;700&display=swap" rel="stylesheet">
    <title>Capstone</title>
</head>
<body>
    <div class="searchCont">
    <form action="/" method="post">
        <input type="search" name="city" class="searchBox" placeholder="City/Town"></textarea>
        <input type="submit" class="button" value="Search" />
    </form>
    </div>

    <% if(locals.weather){   %>
    <h1>Weather Forecast</h1>

    <div class="weath2nd">
        <div class="box1">
            <%  if( weather  === "clearday"){   %>
                <img src="images/sun.gif" alt="Sun">
            <%  }   %>

            <%  if( weather  === "clearnight"){   %>
                <img src="images/night.gif" alt="Night">
            <%  }   %>

            <%  if( weather  === "pcloudyday" || weather  === "pcloudynight" || weather  === "mcloudyday" || weather  === "mcloudynight" || weather  === "cloudyday" || weather  === "cloudynight"){   %>
                <img src="images/clouds.gif" alt="Clouds">
            <%  }   %>

            <%  if( weather  === "humidday" || weather  === "humidnight"){   %>
                <img src="images/humidity.gif" alt="humidity">
            <%  }   %>

            <%  if( weather  === "lightrainday" || weather  === "lightrainnight" || weather  === "oshowerday" || weather  === "oshowernight" || weather  === "ishowerday" || weather  === "ishowernight"|| weather  === "rainday" || weather  === "rainnight"){   %>
                <img src="images/rain.gif" alt="Rain">
            <%  }   %>

            <%  if(weather  === "snowday" || weather  === "snownight" || weather  === "rainsnowday" || weather  === "rainsnownight" || weather  === "lightsnowday" || weather  === "lightsnownight" ){   %>
                <img src="images/snow.gif" alt="Snow">
            <%  }   %>
        </div>

        <div class="box2">
            <p><%= tdate %><p>
            <span class="tday"><%= weather %></span>
        </div>
    </div>

    <div class="box3">
        <p><%= temp %></p><img src="images/celsius.gif" alt="celsius">
    </div>

    <div class="container">
        <div class="box">
            <img src="images/temperature.gif" class="icon" alt="temperature">
            <span>
                <span class="bold">Temp</span>
                <br>
                <span class="value"><%= temp %><img src="images/celsius.gif" class="temp" alt="celsius"></span>
            </span>
        </div>
        <div class="box">
            <img src="images/humidity.gif" class="icon" alt="humidity">
            <span>
                <span class="bold">Humidity</span>
                <br>
                <span class="value"><%= humid %></span>
            </span>
        </div>
        <div class="box">
            <img src="images/wind.gif" class="icon" alt="Wind">
            <span>
                <span class="bold">W-Speed</span>
                <br>
                <span class="value"><%= windSpeed*3 %> m/s <%= windDirec %></span>
            </span>
        </div>
    </div>

 <% }    %>


<%- include("partials/footer.ejs") %>